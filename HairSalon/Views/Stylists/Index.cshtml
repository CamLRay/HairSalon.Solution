@{
  Layout = "_Layout";
}

@using HairSalon.Models;
<h1>Stylists</h1>

@if (@Model.Count == 0)
{
  <h3>Better hire some stylists...</h3>
}

<ul>
  @foreach(Stylist stylist in Model)
  {
    <li>@stylist.Name</li>
  }
</ul>

<p>@Html.ActionLink("Add new stylist", "Create")</p>